import type { Meta, StoryObj } from '@storybook/react';

const meta: TMeta = {
  component: () => <></>
}

export default meta

export const Primary: Story = {
}

type Story = StoryObj
type TMeta = Meta