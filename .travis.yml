language: node_js
node_js: 12
cache: yarn
jobs:
  include:
    - stage: test
      script:
        - 'npm run build'
    - stage: deploy
      script:
        - yarn global add firebase-tools
        - firebase deploy --token=${DESIGN_TOKEN}
